# cwms_esp
Analyze and view data from a Corps Water Management System (CWMS) compute that uses Ensemble Stream Predictions (ESP) traces from the Northwest River Forecast Center (NWRFC).

 [ ] Routing script that can be configured with just the upstream and downstream nodes; steal code from CRT watershed's ResSimFunctions.py
